/* eslint-disable */

const buttonToPermissions = {
    <%
        for (let permissionInfo of permissionInfos) {
          let permissionId = `${permissionInfo.operation}_${permissionInfo.object}`
    %>
    <%=permissionInfo.buttonId%>: '<%=permissionId%>',<%}%>
}

exports.buttonToPermissions = buttonToPermissions
